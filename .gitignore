CMakeLists.txt.user
CMakeCache.txt
CMakeFiles
CMakeScripts
Testing
Makefile
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps
sources/
.vscode/
*.venv
__pycache__
/.ccache
.cline_storage/

# Patch control files.
.*.smrev

# Branch-specific flag overrides (see FLAGS.cmake).
/BRANCH_FLAGS.cmake
/BRANCH_CONFIG.json

# Optional externally fetched sources.
/optional-sources/

# Output directories.
/build/
/install/
/output-*/
/out/

# macOS eGPU out-of-tree build trees (one per agent/library config).
/build-macos-egpu*/
# Validation smoke build outputs and Xcode DriverKit build trees.
/validation/**/build/
/userspace_driver/macos_driver/build/
# Compiled debug smoke binaries (sources are tracked, binaries are not).
/userspace_driver/debug/macgpu_phase10_compute_direct

# DriverKit iig-generated header symlink (points to Xcode DerivedSources;
# re-created by the xcodeproj's "Create generated symlink" build phase).
/userspace_driver/macos_driver/ROCmGPU/ROCmGPUDriver/generated

# Xcode per-user and workspace state inside the xcodeproj.
/userspace_driver/macos_driver/ROCmGPU/ROCmGPU.xcodeproj/xcuserdata/
/userspace_driver/macos_driver/ROCmGPU/ROCmGPU.xcodeproj/project.xcworkspace/
